import { MenusAnglaisPipe } from './menus-anglais.pipe';

describe('MenusAnglaisPipe', () => {
  it('create an instance', () => {
    const pipe = new MenusAnglaisPipe();
    expect(pipe).toBeTruthy();
  });
});
